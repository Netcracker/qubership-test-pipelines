|
  dbaas='
    install: true
    aggregator:
      physicalDatabaseIdentifier: redis
  ';
  redis='
    tls:
      enabled: true
      generateCerts:
        enabled: true
        clusterIssuerName: qa-issuer-self
  ';
  monitoringAgent='
    install: true
    metricCollector: prometheus
  ';
  robotTests='
    install: true
  ';
