| 
  
  CASSANDRA_USERNAME=admin;
  CASSANDRA_KEYSPACE=profiler2;
  CASSANDRA_PORT=9042;
  CASSANDRA_PASSWORD='';
  CASSANDRA_HOST=cassandra.cassandra.svc;
  CASSANDRA_DC=dc1;
  INTEGRATION_TESTS=false;
