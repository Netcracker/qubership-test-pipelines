| 
  
  CASSANDRA_USERNAME=admin;
  CASSANDRA_KEYSPACE=profiler1;
  CASSANDRA_PORT=9042;
  CASSANDRA_PASSWORD='';
  CASSANDRA_HOST=cassandra.cassandra.svc;
  CASSANDRA_DC=dc1;
  INTEGRATION_TESTS=true;
  DIAG_PV_EMPTYDIR=true;

